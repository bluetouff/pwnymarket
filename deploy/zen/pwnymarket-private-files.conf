# Only the Unix-socket proxy may serve the application's allowlisted assets.
# Deny filesystem access through every vhost sharing the Apache document root.
<Directory "/var/www/html/pwnymarket">
    AllowOverride None
    Options None
    Require all denied
</Directory>
